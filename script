//palindrome

echo "enter a number:"
read n
rev=0
temp=$n
while [ $n -gt 0 ] 
do
digit=$(($n % 10))
rev=$(($rev * 10 + $digit))
n=$(($n / 10))
done
echo "$rev"
if [ $temp -eq $rev ]; then
   echo "palindrome"
else
   echo "not palindrome"
fi
